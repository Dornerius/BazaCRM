<local:WszystkieViewBase x:Class="MVVMFirma.Views.WszystkieProjektyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="600">

    <Grid Background="Aqua">
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nazwa Projektu" Binding="{Binding NazwaProjektu}"/>
                <DataGridTextColumn Header="Data Rozpoczecia" Binding="{Binding DataRozpoczecia}"/>
                <DataGridTextColumn Header="Data Zakonczenia" Binding="{Binding DataZakonczenia}"/>               
                </DataGrid.Columns>
        </DataGrid>
    </Grid>
    
</local:WszystkieViewBase>
